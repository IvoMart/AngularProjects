import { OrdenaNombrePipe } from './ordena-nombre.pipe';

describe('OrdenaNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenaNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
