import { QuitaRepePipe } from './quita-repe.pipe';

describe('QuitaRepePipe', () => {
  it('create an instance', () => {
    const pipe = new QuitaRepePipe();
    expect(pipe).toBeTruthy();
  });
});
