import { QuitaRepeAparPipe } from './quita-repe-apar.pipe';

describe('QuitaRepeAparPipe', () => {
  it('create an instance', () => {
    const pipe = new QuitaRepeAparPipe();
    expect(pipe).toBeTruthy();
  });
});
