import { BuscaNombrePipe } from './busca-nombre.pipe';

describe('BuscaNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new BuscaNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
