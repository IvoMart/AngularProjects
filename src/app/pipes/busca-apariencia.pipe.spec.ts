import { BuscaAparienciaPipe } from './busca-apariencia.pipe';

describe('BuscaAparienciaPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscaAparienciaPipe();
    expect(pipe).toBeTruthy();
  });
});
